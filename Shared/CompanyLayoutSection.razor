@* Test version of CompanyLayoutSection to verify parameter binding *@

@if (!IsInitialized)
{
    <div class="alert alert-info">
        <h4>Company Section - Loading</h4>
        <p>IsInitialized: @IsInitialized</p>
        <p>IsRegistered: @IsRegistered</p>
    </div>
}
else if (!IsRegistered)
{
    <div class="alert alert-warning">
        <h4>Company Section - Not Registered</h4>
        <p>IsInitialized: @IsInitialized</p>
        <p>IsRegistered: @IsRegistered</p>
        <button class="btn btn-primary" @onclick="() => SetRegistered(true)">Register as Company</button>
    </div>
}
else
{
    <div class="alert alert-success">
        <h4>Company Section - Registered</h4>
        <p>IsInitialized: @IsInitialized</p>
        <p>IsRegistered: @IsRegistered</p>
        <button class="btn btn-secondary" @onclick="() => SetRegistered(false)">Unregister</button>
    </div>
}
